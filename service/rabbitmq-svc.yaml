apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
spec:
  ports:
  - name: amqp
    port: 5672
    protocol: TCP
    targetPort: 5672
  - port: 15672
    protocol: TCP
    targetPort: 15672
    name: http-web
  selector:
    app: rabbitmq
  sessionAffinity: None
  type: NodePort
